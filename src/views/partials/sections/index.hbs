<section id="index" class="min-h-screen bg-gradient-to-br from-purple-50 to-blue-50 py-20">
    <div class="container mx-auto px-4">
        {{!-- Header del índice --}}
        <div class="text-center mb-16">
            <h1 class="text-5xl lg:text-7xl font-bold mb-6 bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
                {{company.name}}
            </h1>
            <p class="text-xl text-gray-600 max-w-2xl mx-auto">
                {{company.description}}
            </p>
            <div class="mt-8">
                <span class="inline-block px-4 py-2 bg-purple-100 text-purple-800 rounded-full text-sm font-medium">
                    🎨 Estilo: {{style}}
                </span>
            </div>
        </div>

        {{!-- Grid de secciones --}}
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
            {{#each allSections}}
                {{#if enabled}}
                <div class="group">
                    <a href="/api/v1/client-templates/{{../clientId}}/section/{{id}}" 
                       class="block bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 border border-gray-100 overflow-hidden">
                        
                        {{!-- Icono de la sección --}}
                        <div class="h-48 bg-gradient-to-br from-purple-100 to-blue-100 flex items-center justify-center">
                            <div class="text-6xl opacity-80 group-hover:scale-110 transition-transform duration-300">
                                {{#if (eq id "hero")}}🏠
                                {{else if (eq id "about")}}👥
                                {{else if (eq id "products")}}🛍️
                                {{else if (eq id "services")}}⚙️
                                {{else if (eq id "testimonials")}}💬
                                {{else if (eq id "gallery")}}🖼️
                                {{else if (eq id "contact")}}📞
                                {{else if (eq id "cart")}}🛒
                                {{else if (eq id "appointments")}}📅
                                {{else if (eq id "stats")}}📊
                                {{else}}📄
                                {{/if}}
                            </div>
                        </div>
                        
                        {{!-- Contenido de la sección --}}
                        <div class="p-6">
                            <h3 class="text-xl font-bold text-gray-800 mb-2 group-hover:text-purple-600 transition-colors">
                                {{getSectionDisplayName id}}
                            </h3>
                            
                            {{!-- Descripción de la sección --}}
                            <p class="text-gray-600 text-sm mb-4">
                                {{#if (eq id "hero")}}
                                    Página principal con información destacada de la empresa
                                {{else if (eq id "about")}}
                                    Conoce más sobre nosotros y nuestra historia
                                {{else if (eq id "products")}}
                                    Explora nuestro catálogo de productos
                                {{else if (eq id "services")}}
                                    Descubre los servicios que ofrecemos
                                {{else if (eq id "testimonials")}}
                                    Opiniones y experiencias de nuestros clientes
                                {{else if (eq id "gallery")}}
                                    Galería de imágenes de nuestros trabajos
                                {{else if (eq id "contact")}}
                                    Información de contacto y formularios
                                {{else if (eq id "cart")}}
                                    Carrito de compras y gestión de pedidos
                                {{else if (eq id "appointments")}}
                                    Reserva de citas y agendamiento
                                {{else if (eq id "stats")}}
                                    Estadísticas y logros de la empresa
                                {{else}}
                                    Contenido personalizado de la sección
                                {{/if}}
                            </p>
                            
                            {{!-- Estado de la sección --}}
                            <div class="flex items-center justify-between">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    ✅ Activa
                                </span>
                                <span class="text-xs text-gray-500">
                                    Orden: {{order}}
                                </span>
                            </div>
                        </div>
                        
                        {{!-- Indicador de hover --}}
                        <div class="h-1 bg-gradient-to-r from-purple-500 to-blue-500 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300"></div>
                    </a>
                </div>
                {{/if}}
            {{/each}}
        </div>

        {{!-- Información adicional --}}
        <div class="mt-16 text-center">
            <div class="bg-white rounded-2xl shadow-lg p-8 max-w-4xl mx-auto">
                <h2 class="text-2xl font-bold text-gray-800 mb-4">📋 Información del Sitio</h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-sm">
                    <div>
                        <div class="font-semibold text-gray-700 mb-2">📊 Estadísticas</div>
                        <div class="text-gray-600">
                            <div>• {{allSections.length}} secciones totales</div>
                            <div>• {{#each allSections}}{{#if enabled}}{{add @index 1}}{{/if}}{{/each}} secciones activas</div>
                            <div>• Estilo: {{style}}</div>
                        </div>
                    </div>
                    <div>
                        <div class="font-semibold text-gray-700 mb-2">🎨 Personalización</div>
                        <div class="text-gray-600">
                            <div>• Colores personalizados</div>
                            <div>• Tipografías adaptadas</div>
                            <div>• Diseño responsivo</div>
                        </div>
                    </div>
                    <div>
                        <div class="font-semibold text-gray-700 mb-2">🔧 Funcionalidades</div>
                        <div class="text-gray-600">
                            <div>• Navegación por teclado</div>
                            <div>• Enlaces directos</div>
                            <div>• Diseño optimizado</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {{!-- Navegación rápida --}}
        <div class="mt-12 text-center">
            <div class="flex flex-wrap justify-center gap-4">
                <a href="/api/v1/client-templates/{{clientId}}" 
                   class="px-6 py-3 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors">
                    🌐 Ver sitio completo
                </a>
                <a href="/api/v1/client-templates/{{clientId}}/configuration" 
                   class="px-6 py-3 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition-colors">
                    ⚙️ Configuración
                </a>
            </div>
        </div>
    </div>
</section>

{{!-- Ejemplo de cómo acceder a datos de secciones específicas --}}
<script>
// Ejemplo de cómo acceder a todos los datos de las secciones desde JavaScript
document.addEventListener('DOMContentLoaded', function() {
    // Los datos de todas las secciones están disponibles en la variable global 'allSections'
    console.log('Todas las secciones:', window.allSections);
    
    // Ejemplo: Obtener datos de una sección específica
    const heroSection = window.allSections?.find(section => section.id === 'hero');
    if (heroSection) {
        console.log('Datos de Hero:', heroSection.data);
    }
    
    // Ejemplo: Obtener todas las secciones deshabilitadas
    const disabledSections = window.allSections?.filter(section => !section.enabled);
    console.log('Secciones deshabilitadas:', disabledSections);
});
</script>


